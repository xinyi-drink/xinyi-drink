# 注意事项

- 推荐由 Agent 自行完成。
- 推荐能力统一走 `/skill/xinyi/context`；这个接口直接返回商品、门店、天气和可选订单历史。
- 接口返回的是结构化原始数据；提供给大模型前，应使用脚本层整理成文本/表格，不要把 JSON 原样塞给模型。
- 用户输入过手机号后，默认复用本地保存的手机号，除非用户明确要求更换或清空。
- 天气是增强输入，不是硬依赖；Skill 内默认查询北京，用户主动问天气时也可以查任意地点；天气失败时会降级。
- 门店状态缓存只有 5 分钟，不保证秒级实时。
